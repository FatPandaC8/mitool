name: basic

structure:
  - name: cmd
    type: directory
    children:
      - name: main.go
        type: file

  - name: internal
    type: directory

  - name: configs
    type: directory

  - name: docs
    type: directory

  - name: scripts
    type: directory

  - name: tests
    type: directory

  - name: examples
    type: directory

files:
  - README.md
  - Makefile
  - .gitignore
